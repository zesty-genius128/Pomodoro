class AppConstants {
  static const Duration defaultPomodoroDuration = Duration(minutes: 25);
  static const Duration defaultShortBreakDuration = Duration(minutes: 5);
  static const Duration defaultLongBreakDuration = Duration(minutes: 15);
  
  static const int longBreakInterval = 4; // After every 4 pomodoros
  
  static const double timerSize = 280.0;
  static const double timerStrokeWidth = 12.0;
  
  static const Duration animationDuration = Duration(milliseconds: 300);
}